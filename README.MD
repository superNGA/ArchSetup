Packages.txt -> Collection of packages that need to be explicitly installed.
                Dumped using "pacman -Qqe"
                install using "sudo pacman -S --needed --noconfirm $(cat Packages.txt)"
